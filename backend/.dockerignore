# Exclude compiled output (will be built inside Docker)
**/target/

# Exclude IDE/OS files
.idea/
*.iml
.git/
**/.git/

# Exclude local scripts (not needed in Docker)
run-service.ps1
copy-dependencies.ps1

# Exclude pre-built lib folder (no longer used in multi-stage builds)
lib/
